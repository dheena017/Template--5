# Backend Core Configurations

- **`db.py`**: Creates the SQLAlchemy `Engine` and sets up the active database session. If you need to change the database from SQLite to PostgreSQL, you modify the connection string here.\n