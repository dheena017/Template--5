# Backend Routes

FastAPI HTTP endpoints. These map directly to URLs fetched by the frontend:
- **`assets.py`**: Handles incoming file uploads and distributions.
- **`pdf.py`**: Receives requests to merge, split, and optimize PDFs.
- **`profile.py`**: User-related endpoints.\n