# backend/api

Contains the main API logic for the backend, including:
- `models/`: SQLAlchemy ORM models for database tables.
- `routes/`: FastAPI route definitions for all endpoints.
- `services/`: Business logic and service layers.
