# Backend API Directory

This is where the actual logic of the backend lives, separated into three strict layers:
1. **`models/`**: Defines what data looks like in the database.
2. **`routes/`**: Receives incoming HTTP requests from the frontend.
3. **`services/`**: Performs the business logic triggered by route endpoints.\n