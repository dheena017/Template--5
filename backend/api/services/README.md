# Backend Services

Contains reusable business logic so routes stay clean:
- **`analytics_service.py`**: Handles metric gathering.
- **`pdf_service.py`**: Uses Python libraries to actually fulfill the PDF modifications requested by the routes.\n