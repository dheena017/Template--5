# Features Directory

Specialized tool logic and complex, state-heavy interactive sections that are too big to just be a "component".
For example, `pdf/` contains the actual logic/UI integration for manipulating a PDF directly via `pdf-lib` in the browser before rendering it on the page.\n