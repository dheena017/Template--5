import React from 'react';
import '../../styles/buttons/SuccessButton.css';
import BaseButton from './BaseButton';

const SuccessButton = (props) => <BaseButton {...props} variant="success" />;

export default SuccessButton;
