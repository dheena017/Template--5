import React from 'react';
import '../../styles/buttons/DangerButton.css';
import BaseButton from './BaseButton';

const DangerButton = (props) => <BaseButton {...props} variant="danger" />;

export default DangerButton;
