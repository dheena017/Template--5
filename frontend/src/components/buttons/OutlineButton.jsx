import React from 'react';
import '../../styles/buttons/OutlineButton.css';
import BaseButton from './BaseButton';

const OutlineButton = (props) => <BaseButton {...props} variant="outline" />;

export default OutlineButton;
