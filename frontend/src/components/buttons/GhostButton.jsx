import React from 'react';
import '../../styles/buttons/GhostButton.css';
import BaseButton from './BaseButton';

const GhostButton = (props) => <BaseButton {...props} variant="ghost" />;

export default GhostButton;
